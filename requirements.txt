# Core ML / embedding (offline — no network required)
scikit-learn>=1.4.0
numpy>=1.26.0

# Vector search
faiss-cpu>=1.8.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Utilities
tabulate>=0.9.0

# Optional: swap in for production-grade embeddings when network is available
# sentence-transformers>=2.6.0
# torch>=2.2.0
